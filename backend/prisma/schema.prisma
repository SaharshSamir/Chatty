// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Message {
  messageId String @id @default(cuid())
  text      String
  sender    User   @relation(name: "MessageToUser", fields: [userId], references: [userId])
  userId    String
}

model User {
  userId   String    @id @default(cuid())
  Message  Message[] @relation(name: "MessageToUser")
  username String
  email    String
}
